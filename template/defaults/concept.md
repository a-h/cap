# Name the concept as the domain noun, for example "Policy"

<!-- A concept is a thing in the bounded context's ubiquitous language: a domain noun the capabilities operate on, such as a policy, a decision, or a request. Define it so the language of the context is written down rather than left implicit in capability names. Where this concept's name appears in another entity, tag it with its identifier, for example "Policy (con-0001)", so a reference can be traced. -->

## Metadata (optional)

- context:

## Definition

State what this thing is in a sentence or two, in the language of the domain. Define
it independently of how it is stored or represented in any technology.

## States (optional)

The lifecycle states this thing can be in, if it has any, for example draft, active,
or expired.

## Relationships (optional)

How this concept relates to other concepts in the context, by identifier, for example
"a decision is the outcome of evaluating a policy (con-0002)".
